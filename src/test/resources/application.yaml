spring:
  application:
    name: test-profiled-properties
  main:
    cloud-platform: kubernetes
  config:
    import: "kubernetes:"
  cloud:
    kubernetes:
      config:
        sources:
          - name: my-source
            namespace: default
            include-profile-specific-sources: true
